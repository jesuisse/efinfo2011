
Datenbank aus facebook.sql erstellen:

$ sqlite3 facebook.db
sqlite> .read facebook.sql
sqlite> .tables
person status sindBefreundet wallposts
sqlite> .exit
$

Wenn auf der Kommandozeile nichts angegeben wird, kann auch mittels .backup eine Datei erzeugt werden.



 